function depuracaoSemDebugger() {
    console.log("\n===== EXERCÍCIO 14 =====");

    console.log(
        "Para continuar a execução depois de atingir um breakpoint, utilizamos o botão 'Resume script execution' ou pressionamos F8."
    );

    console.log(
        "Para remover todos os breakpoints, podemos acessar o painel de breakpoints do DevTools e utilizar a opção para desativar ou remover todos os breakpoints."
    );

    console.log(
        "Também é possível clicar novamente nos pontos marcados no código para removê-los individualmente."
    );
}

depuracaoSemDebugger();