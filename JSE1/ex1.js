function definicoesBasicas() {
    console.log("===== EXERCÍCIO 1 =====");

    console.log("1. Erro:");
    console.log(
        "Um erro é um problema no código que pode fazer o programa não funcionar como esperado."
    );

    console.log("2. Exceção:");
    console.log(
        "Uma exceção é uma situação inesperada durante a execução do programa que pode ser tratada usando try...catch."
    );

    console.log("3. Diferença:");
    console.log(
        "Na linguagem natural, erro significa que algo deu errado. Em JavaScript, uma exceção é um problema detectado durante a execução que pode ser lançado e tratado pelo programa."
    );
}

definicoesBasicas();